# Copy this file to "keystore.properties" (same folder) and fill in your real
# values. NEVER commit the real keystore.properties or the .jks file it points
# to — both are already in .gitignore.

storeFile=../driverapp-release-key.jks
storePassword=REPLACE_ME
keyAlias=driverapp
keyPassword=REPLACE_ME
